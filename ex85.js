function One(a){
    var Arr = ['meat', 'fish', 'vegetable', 'apple', 'salt', 'chicken',' pear'];
    let cnt=Arr.length;
    alert('Длина массива: ' +  cnt);
    for (var i = 0; i < Arr.length; i++) {
          alert('Элемент массива №  ' + i + '  ' + Arr[i]);        
}
        return;
}
//alert(One(1));